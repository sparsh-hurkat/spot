import { Theme } from "@mui/material";

const styles = (theme: Theme) => {
  return {
    carousel: {
      width: "90%",
      height: "500px",
      margin: "0 auto",
    },
  };
};

export default styles;
